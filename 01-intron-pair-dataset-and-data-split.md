# 从剪接位点预测到 Intron 配对：数据集构建与划分
最近在做酵母菌转录预测中的 RNA 剪接部分。最开始我的思路比较简单：分别训练 Donor 和 Acceptor 两个 CNN，让模型找到可能的剪接位点，再根据这些位置确定 intron。
## 1. Donor 和 Acceptor CNN 到底解决了什么？
目前我分别训练了 Donor CNN 和 Acceptor CNN。
模型输入是候选剪接位点附近的 101 bp DNA 序列，先通过 One-hot 编码转换成 `4 × 101` 的矩阵，再经过一维卷积神经网络提取序列特征，最后完成二分类：
- `0`：不是真实剪接位点
- `1`：是真实剪接位点
所以这两个 CNN 实际解决的问题是：
> **这个位置像不像一个真正的剪接位点？**
而不是：
> **这个 Donor 应该和哪个 Acceptor 组成一个 intron？**
这两个问题一开始我其实混在了一起。
## 2. 实际测试后发现：找到剪接位点还不够
为了检查 Donor 和 Acceptor 模型放到真实序列中到底是什么效果，我用 ACT1 做了一次测试。
ACT1 中真实的 intron 对应的 Donor 和 Acceptor 大约位于：
`110 → 417`
模型确实成功找到了这两个位置：
- Donor 110：预测概率约 0.8987
- Acceptor 417：预测概率约 0.9280
这说明两个模型具有识别真实剪接位点的能力。
但问题也很明显：模型同时预测出了很多其他高概率候选位置。例如 Donor 还出现了 114、144 等候选位置，Acceptor 也出现了 419 等候选位置。
更有意思的是，有些错误候选位置的预测概率甚至比真实位置更高。例如 Donor 144 的概率约为 0.9244，高于真实 Donor 110；Acceptor 419 的概率约为 0.9446，也高于真实 Acceptor 417。
所以如果直接采用“Donor 选概率最高的 + Acceptor 选概率最高的”这种方法，很可能会得到错误的 intron。
做到这里我才真正区分开两个任务：
> **剪接位点预测解决的是“哪些位置可能是剪接位点”，而 Intron 配对解决的是“这些候选位置中，哪两个才应该组成一个真正的 Intron”。**
因此，我决定在 Donor CNN 和 Acceptor CNN 后面再增加一层 Intron 配对判断。
> ## 3. 为 Intron 配对模型重新构建数据集
确定需要增加 Intron 配对模型之后，下一个问题就是：这个模型拿什么数据训练？
我先从酵母菌的 protein-coding transcript 注释出发，根据相邻 exon 的位置反推出真实 intron。为了先把问题控制在比较明确的范围内，目前只保留 canonical GT-AG intron。
经过筛选后，一共得到了：
- 270 个真实 canonical intron
- 每一个真实 Donor-Acceptor 配对作为一个正样本
- 因此正样本一共有 270 条，`label = 1`
但只有正确答案显然不够，模型还必须学习什么叫“错误配对”，所以接下来还需要构造负样本。
### 负样本怎么生成？
我没有直接随机生成一些完全无关的位置，而是尽量构造更容易和正确答案混淆的错误配对。
主要用了两种方式：
1. 保留真实 Donor，在 intron 内部寻找其他 AG，与错误的 Acceptor 组成配对；
2. 保留真实 Acceptor，在 intron 内部寻找其他 GT，与错误的 Donor 组成配对。
同时限制候选 intron 的长度范围，并优先选择长度与真实 intron 比较接近的错误配对。每个真实 intron 最多保留 2 个这样的负样本。
这样做的原因是，如果负样本和正样本差别特别明显，模型可能很容易得到一个看起来很高的准确率，但实际上并没有学会区分那些真正容易混淆的候选配对。
这种比较难区分的负样本也可以理解为 **Hard Negative（困难负样本）**。
最终生成的数据为：
| 类型 | 数量 |
| --- | ---: |
| 正样本 | 270 |
| 负样本 | 511 |
| 总样本 | 781 |
这里的 781 并不是提前规定好的数据量。
理论上每个真实 intron 最多可以生成 2 个负样本，但并不是每个 intron 内部都能找到两个满足条件的错误 GT/AG，因此最后实际得到的是 511 个负样本，最终总数自然变成了 781。
## 4. 第一版数据做完后，我为什么又改了一次？
781 条数据生成以后，我没有马上拿去训练，而是先写了检查程序，对整个配对数据集做了一次检查。
主要检查了：
- 正负样本数量是否正常
- 是否存在缺失值
- intron 长度分布是否合理
- 是否存在完全重复的数据
- branchpoint 的出现情况和位置
- label 是否只有 0 和 1
检查结果显示，数据整体结构没有明显问题：没有重复样本，intron 长度也都在设定范围内。不过在重新考虑后续真实预测流程时，我发现第一版数据的特征设计还有一些不足。
### 问题 1：错误位点没有保存真实坐标
第一版数据中，错误的候选位置使用的是：
`false_internal_donor`
和
`false_internal_acceptor`
这样的字符串。
这在生成数据时可以帮助我区分负样本类型，但如果后面真的要把 Donor、Acceptor 和其他模型连接起来，就需要知道候选位点在基因组中的实际位置。
因此后面重新计算了正负链情况下的真实基因组坐标，让正样本和负样本都保存真正的 `donor_position` 和 `acceptor_position`。
### 问题 2：只看长度和 branchpoint 信息还不够
第一版配对数据主要依赖：
- intron_length
- branchpoint
- branchpoint_distance
但两个长度接近、branchpoint 也相似的候选 intron，仍然可能一个是真的、一个是假的。
因此第二版又增加了更多序列信息：
- `donor_sequence`：Donor 附近的局部序列
- `acceptor_sequence`：Acceptor 附近的局部序列
- `branchpoint_present`：是否检测到 branchpoint
- `candidate_intron_sequence`：整个候选 intron 的序列
这样配对模型以后就不只是看到“这个 intron 有多长”，而是能够同时利用候选 intron 本身的序列特征。
### 这次修改让我意识到的一点
以前我会觉得“数据已经生成成功、代码没有报错”就代表这一步做完了。
但这次比较明显地感觉到：
> **数据能被程序读取，不等于这些数据真的适合模型学习。**
除了检查格式和缺失值，还需要考虑这些特征到了真实预测阶段能不能获得、是否真的能够帮助模型区分正负样本，以及训练数据和实际使用场景是否一致。
所以相比第一版，我更愿意把第二版看成是从“能训练的数据”向“更接近实际预测任务的数据”做的一次调整。
> ## 5. 训练集、验证集和测试集到底有什么区别？
数据集重新整理好以后，我又遇到了一个之前经常听到、但其实没有真正搞明白的问题：为什么机器学习的数据要分成训练集、验证集和测试集？
这次结合自己的数据，我终于把三者的作用理清楚了。
### 训练集（Train Set）
训练集是真正用来让模型“学习”的数据。
模型会根据训练样本不断计算预测结果和真实标签之间的误差，再通过训练过程更新模型参数。
简单来说：
> **训练集是模型平时真正拿来学习和改参数的数据。**
这次一共有 501 条训练样本，其中：
- 328 条负样本
- 173 条正样本
### 验证集（Validation Set）
验证集不会直接拿来更新模型参数。
它更像训练过程中的“模拟考试”。训练过程中可以用验证集观察模型面对没有直接参与参数更新的数据时表现怎么样，并据此比较不同模型、不同参数或者不同训练轮次。
例如以后训练 Intron 配对模型时，如果模型在训练集上的效果一直提高，但验证集效果反而开始下降，就需要警惕模型是不是开始过拟合了。
这次验证集包含 150 条数据：
- 99 条负样本
- 51 条正样本
- 来自 IV、VIII 号染色体
### 测试集（Test Set）
测试集更像最后的“期末考试”。
它不应该被反复拿来调整模型，否则我实际上就会慢慢根据测试结果修改模型，最后测试集也相当于参与了模型开发，最终得到的成绩就不能很好地代表模型面对真正未知数据时的表现。
所以测试集应该尽量留到模型和主要参数确定以后，再进行最终评价。
这次测试集一共有 130 条数据：
- 84 条负样本
- 46 条正样本
- 来自 VII、XII 号染色体
目前的数据划分结果为：
| 数据集 | 样本数 | 负样本 | 正样本 |
| --- | ---: | ---: | ---: |
| Train | 501 | 328 | 173 |
| Validation | 150 | 99 | 51 |
| Test | 130 | 84 | 46 |
| Total | 781 | 511 | 270 |
## 6. 为什么没有直接随机划分？
一开始我会觉得，把 781 条数据随机分成训练集、验证集和测试集不就可以了吗？
后来才发现，对于现在这个数据集来说，这样做存在一个比较重要的问题。
一个真实 intron 不仅会产生一个正样本，还可能围绕它生成多个困难负样本。这些样本来自同一个 transcript 或相同的局部序列区域，因此彼此之间可能非常相似。
如果直接随机划分，就可能出现：
- 某个真实配对进入训练集
- 和它非常相似的错误配对进入测试集
这样虽然它们不是完全相同的一条数据，但模型其实已经在训练阶段见过非常相似的序列环境。
最后得到的测试成绩可能会比模型真正面对未知区域时的表现更好，这就是我这次开始真正理解的 **Data Leakage（数据泄漏）** 问题之一。
所以最后没有采用简单的逐条随机划分，而是按照染色体进行隔离：
- Train：I、II、III、V、VI、IX、X、XI、XIII、XIV、XV、XVI
- Validation：IV、VIII
- Test：VII、XII
这样测试集中的 VII、XII 号染色体不会出现在训练集中，验证集的 IV、VIII 号染色体也与训练集分开。
相比单纯随机拆分样本，这种方式可以更严格地检查模型面对训练阶段没有见过的染色体数据时还能不能做出有效判断。
这也让我第一次比较具体地意识到：
> **一个模型的测试结果是否可信，不只取决于模型本身，也取决于数据是怎么划分的。**
如果训练集和测试集之间存在信息泄漏，再高的 Accuracy 或 F1 也可能不能代表模型真正的泛化能力。
> ## 7. 现在整个剪接预测的思路
经过前面的测试和数据调整以后，我现在对整个剪接预测流程的理解比一开始清楚了很多。
目前的思路可以概括成：
DNA 序列  
↓  
寻找候选 GT / AG 位点  
↓  
Donor CNN / Acceptor CNN  
↓  
判断各候选位置像不像真实剪接位点  
↓  
得到多个候选 Donor 和 Acceptor  
↓  
Intron Pair Model  
↓  
判断哪个 Donor 应该和哪个 Acceptor 配对  
↓  
确定 Intron  
↓  
去除 Intron  
↓  
得到成熟 RNA  
↓  
进行后续蛋白质预测
这里 Donor CNN 和 Acceptor CNN 负责的是“找点”，而后面的 Intron Pair Model 负责的是“配对”。
这也是我这次做下来最大的思路变化之一。最开始我以为只要把 Donor 和 Acceptor 分别预测准确，就基本解决了剪接问题。但实际放到完整序列中测试后才发现，单个位点分类只是整个任务中的一步。
## 8. 今天比较重要的几个收获
这次整理数据和测试模型的过程中，有几个以前只是听过、现在才真正理解的概念。
### 1）模型能运行，不代表整个任务已经解决
Donor CNN 和 Acceptor CNN 都可以正常输出预测结果，但真实序列中会同时出现很多候选位点。
所以一个局部分类模型表现不错，并不代表把它放进完整流程以后就一定能直接得到正确结果。
### 2）数据怎么构造和模型本身一样重要
一开始我更多关注 CNN 怎么搭、Accuracy 有多少。
做到 Intron 配对以后才发现，正负样本怎么定义、负样本难不难、特征有没有实际意义，都会直接影响模型最后学到什么。
尤其是 Hard Negative，让我开始意识到负样本并不是“随便找一些错误答案”就可以。
### 3）第一次真正理解 Validation Set
以前知道机器学习经常分 Train、Validation 和 Test，但对 Validation 的作用比较模糊。
这次实际划分以后，我更愿意把它理解成：
- Train：学习和更新参数
- Validation：开发过程中的模拟考试
- Test：最后尽量只使用一次的期末考试
Validation 的意义不是单纯“再测一次准确率”，而是帮助我在不碰最终 Test Set 的情况下进行模型选择。
### 4）开始注意 Data Leakage
如果只是随机拆分数据，很容易忽略不同样本之间本身存在的联系。
这次因为一个真实 Intron 可以产生多个非常相似的正负配对，所以改成按染色体隔离 Train、Validation 和 Test。
这让我意识到，评价一个机器学习模型时不能只看最后的数字，还要先问一句：
> 这个测试集真的包含模型没有见过的信息吗？
## 9. 下一步
目前已经完成了 Intron 配对数据的生成、检查、特征调整以及 Train / Validation / Test 的染色体级划分。
接下来准备把已经训练好的 Donor CNN 和 Acceptor CNN 真正接入配对流程。
目前两个 CNN 都可以根据候选位点附近的 101 bp DNA 序列判断该位置属于真实剪接位点的可能性，因此下一步准备进一步得到：
- `donor_score`
- `acceptor_score`
并把这两个结果与 Intron 长度、Donor / Acceptor 序列、branchpoint 等信息结合起来，作为 Intron 配对模型的输入。
这里还有一个需要先确认的问题：Donor 和 Acceptor CNN 训练时使用的都是 101 bp 窗口，所以在正式计算 score 之前，需要重新确认训练数据生成时候选位点在 101 bp 窗口中的具体位置，保证推理阶段的截取方式与训练阶段完全一致。
暂时不急着为了让模型跑起来随便定义窗口。
先保证数据和输入逻辑是对的，再继续训练。
